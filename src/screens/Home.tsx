// A simple functional component
const Home = () => {
  return (
    <div>
      <h1>Hello from my first component!</h1>
      <p>This is a starter component using JSX.</p>
    </div>
  );
};

// Export the component so it can be used elsewhere
export default Home;